# Screenshots

Capture these from a real pipeline run (see `../../HOWTORUN.md`):

| File | What it shows |
|------|---------------|
| `pipeline-run.png` | Terminal output of `npm run pipeline` — the per-agent execution log (agent, model, in/out, PASS). |
| `fixes.png` | The applied fixes (diff or the corrected `transform.ts` / `UserTable.tsx`). |
| `security-report.png` | The generated `security-report.md`. |
| `tests-passing.png` | `npm test` after the pipeline — 8 passing. |
