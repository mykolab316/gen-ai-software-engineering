# Transaction Processing Pipeline — Python dependencies
# Install:  python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

fastapi>=0.115
uvicorn>=0.30
fastmcp>=2.0
pytest>=8.0
pytest-cov>=5.0
